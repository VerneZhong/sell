# 服务名称
spring:
  application:
    name: user
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    service-url:
#      defaultZone: http://192.168.1.105:8761/eureka/
      defaultZone: http://localhost:8761/eureka/