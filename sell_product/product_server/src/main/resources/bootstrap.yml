# 服务名称
spring:
  application:
    name: product
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
  zipkin:
    base-url: http://localhost:9411/
    sender:
      type: web
# 服务注册
eureka:
  client:
    service-url:
      defaultZone: http://192.168.1.105:8761/eureka/